# Звіт: генерація 53 рівнів `levels.json`

## Результат валідації

```
Рівнів: 53
ПОМИЛКИ: 0
```

Перевірено: кількість/унікальність `level` (1..53), обов'язкові поля,
`target_type`/`target_details` за типом, `tile_weights` (9 ключів, сума 1.00±0.01,
кожне >0, інструменти ≥0.03), `timer_seconds` 60..180, підмножини `tools`/`allowed_helpers`,
сумарні перешкоди ≤16, межі `cells` 0..6.

### Попередження (не блокують приймання, але варто знати)

1. **L44**: max/min `tile_weights` = 4.00 > 3.5. Причина — таблиця розділу 5 explicitly
   задає «спеції 0.20» на хвилі 9 (база spice=0.12), що при перерахунку дає ratio трохи
   вище порогу з розділу 6. Значення взято дослівно з таблиці; якщо потрібно вкластися
   в 3.5 — можна знизити акцент до ~0.18.
2. **Повтори `target_type`+перешкоди на сусідніх рівнях** (L1–L8, L9/L10, L15/L16, L19/L20):
   це властивість самої таблиці ТЗ (хвиля 1 навчальна — усі `cakes` без перешкод;
   у хвилях 3–4 сусідні рівні по таблиці мають однаковий набір типів перешкод, хоч і різну
   кількість). Я не змінював склад/типи перешкод відносно таблиці розділу 5, оскільки вона
   позначена як «джерело правди». Якщо це небажано — потрібна правка самої таблиці.

## Ключові припущення (там, де ТЗ не давало точного числа)

- **`ingredient_goals`**: додавав лише для рівнів з `target_type: "cakes"` — за формулою
  розділу 4.7 (`flour≈N×2, milk≈N×2, butter≈N×1.5, berries≈N`, округлення до цілого),
  з підміною конкретного інгредієнта, якщо таблиця його явно позначає (L4 berries 8,
  L6 milk 12, L14 berries 10). Для `coffee`/`guests`/`combo_target`/`layered_cake`
  поле не додавав — це узгоджується з тестовими рівнями 1–4 у вихідному `levels.json`
  (жоден не-`cakes` рівень там не мав `ingredient_goals`), а `guests` вже має власні
  цілі в `target_details`.
- **Акценти без явного числа в таблиці** (L6 «акцент молока», L24 «акцент спецій»):
  застосував +0.04 до базового значення хвилі (0.20→0.24 для молока; 0.15→0.19 для спецій).
  Там, де таблиця дає конкретне число (L8 кава 0.14, L37 кава 0.13, L38 спеції 0.18,
  L39 борошно 0.20, L40 масло 0.17, L44 спеції 0.20) — взято дослівно.
- **`description`**: короткий текст українською на основі колонки «Фішка»/«Мета» таблиці —
  ТЗ не задає точний текст, лише що це підказка гравцю.
- **Перешкоди**: всюди формат `{ "type", "count" }` (випадкова розкладка), без `cells` —
  таблиця розділів 1–53 фіксованих позицій не вимагає (п. 4.4.3 ТЗ).
- **tile_weights для L1** (без інструментів): базові ваги хвилі включають rollingpin/spatula
  на рівні бази (0.04 кожен), навіть попри порожній `tools: []` — бо валідатор (п. 7.4)
  вимагає рівно 9 ключів, усі > 0, а правило «≥0.03» стосується лише випадку, коли
  інструмент присутній у `tools`.

## Порядок дій далі

1. Переглянь попередження вище — якщо є заперечення щодо L44 або повторів хвилі 1/3/4,
   скажи, і я перерахую.
2. Тестовий рівень 0 (з поточного `levels.json`) у новому файлі відсутній — його видалення
   віднесено до зони відповідальності власника проекту (розділ 10 ТЗ), нове `levels.json`
   містить лише 1–53.
3. Рекомендую прогнати `node validate_levels.js levels.json`, якщо такий скрипт існує в
   репозиторії гри — я відтворив його правила вручну (Python), але оригінальний скрипт
   може мати додаткові перевірки, не описані текстом ТЗ.
